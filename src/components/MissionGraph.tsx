// The primary graph workspace now uses the system-topology projection inspired by
// CPCayson/x. It consumes the same evidence-backed MANTAS graph and preserves the
// existing WorkbenchShell contract while replacing the older crowded canvas.
export { SystemTopologyGraph as MissionGraph } from './SystemTopologyGraph';
